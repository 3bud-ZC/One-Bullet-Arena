{
  "name": "one-bullet-arena",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "description": "A clean one-bullet wave survival game with one automatically expanding arena.",
  "scripts": {
    "check": "node --check src/main.js && node --check src/game.js && node --check src/game-data.js && node --check src/arena.js && node --check src/audio.js && node --check sw.js && node --check playwright.config.js",
    "test": "node --test tests/core-loop.test.js",
    "test:browser": "playwright test",
    "verify": "npm run check && npm test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  },
  "engines": {
    "node": ">=20"
  }
}
