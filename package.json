{
  "name": "one-bullet-arena",
  "version": "2.4.1",
  "private": true,
  "type": "module",
  "description": "A stable one-bullet wave survival game with one focused progression path.",
  "scripts": {
    "check": "node --check src/main.js && node --check src/game.js && node --check src/game-runtime.js && node --check src/game-data.js && node --check src/arena.js && node --check src/audio.js && node --check src/input-controller.js && node --check src/spawn-system.js && node --check src/ui-renderer.js && node --check sw.js && node --check playwright.config.js",
    "test": "node --test tests/*.test.js",
    "test:browser": "playwright test",
    "verify": "npm run check && npm test",
    "verify:all": "npm run verify && npm run test:browser"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  },
  "engines": { "node": ">=20" }
}
