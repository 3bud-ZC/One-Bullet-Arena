{
  "name": "one-bullet-arena",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "description": "Arabic browser action game built around one recoverable ricochet bullet.",
  "scripts": {
    "check": "node --check src/main.js && node --check src/game.js && node --check src/math.js && node --check src/audio.js && node --check src/content.js && node --check src/input.js",
    "test": "node --test tests/*.test.js",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
