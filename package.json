{
  "name": "one-bullet-arena",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A fast top-down browser action game built around one recoverable bullet.",
  "scripts": {
    "check": "node --check src/main.js && node --check src/game.js && node --check src/math.js",
    "test": "node --test tests/*.test.js",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
